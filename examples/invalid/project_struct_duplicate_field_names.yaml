# ARForge: Invalid example fixture
# Domain: project-level validation.
# Tests validation failure for struct duplicate field names.
autosar:
  version: "4.2"
  rootPackage: "DEMO"

inputs:
  baseTypes: "../platform/base_types.yaml"
  implementationDataTypes: "types/implementation_types_struct_duplicate_field_names.yaml"
  applicationDataTypes: "../types/application_types.yaml"
  units:
    - "../units/*.yaml"
  compuMethods:
    - "../compu_methods/*.yaml"
  interfaces:
    - "../interfaces/*.yaml"
  swcs:
    - "../swcs/*.yaml"
  system: "../system.yaml"


