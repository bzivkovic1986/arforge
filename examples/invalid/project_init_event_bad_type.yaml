# ARForge: Invalid example fixture
# Domain: project-level validation.
# Tests validation failure for init event bad type.
autosar:
  version: "4.2"
  rootPackage: "DEMO"

inputs:
  baseTypes: "../types/base_types.yaml"
  implementationDataTypes: "../types/implementation_types.yaml"
  applicationDataTypes: "../types/application_types.yaml"
  interfaces:
    - "../interfaces/*.yaml"
  swcs:
    - "swcs/SpeedSensor_init_event_bad_type.yaml"
    - "../swcs/SpeedConsumer.yaml"
  system: "../system.yaml"


