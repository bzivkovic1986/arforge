# ARForge: Invalid example fixture
# Domain: project-level validation.
# Tests warning for declared client-server provides port with no operationInvokedEvents.
autosar:
  version: "4.2"
  rootPackage: "DEMO"

inputs:
  baseTypes: "support/base_types.yaml"
  implementationDataTypes: "support/implementation_types.yaml"
  applicationDataTypes: "support/application_types.yaml"
  units:
    - "support/units.yaml"
  compuMethods:
    - "support/compu_methods.yaml"
  interfaces:
    - "../interfaces/If_Diagnostics.yaml"
  swcs:
    - "swcs/DiagServerUnused.yaml"
    - "swcs/DiagClientOnly.yaml"
  system: "system_single_diag_server_unused.yaml"
