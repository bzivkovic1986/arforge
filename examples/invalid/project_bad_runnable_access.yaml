autosar:
  version: "4.2"
  rootPackage: "DEMO"

inputs:
  datatypes: "../datatypes.yaml"
  interfaces:
    - "../interfaces/*.yaml"
  swcs:
    - "../swcs/SpeedSensor.yaml"
    - "swcs/SpeedConsumer_bad_runnable_access.yaml"
  system: "../system.yaml"
