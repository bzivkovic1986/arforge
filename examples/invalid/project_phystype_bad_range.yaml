# ARForge: Invalid example fixture
# Domain: project-level validation.
# Tests validation failure for phystype bad range.
autosar:
  version: "4.2"
  rootPackage: "DEMO"

inputs:
  baseTypes: "platform/base_types_split.yaml"
  implementationDataTypes: "types/implementation_types_split.yaml"
  applicationDataTypes: "types/application_types_phystype_valid.yaml"
  units:
    - "units/*.yaml"
  compuMethods:
    - "compu_methods/speed_linear_bad_range.yaml"
  interfaces:
    - "../interfaces/*.yaml"
  swcs:
    - "../swcs/*.yaml"
  system: "../system.yaml"


