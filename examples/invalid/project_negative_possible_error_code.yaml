# ARForge: Invalid example fixture
# Domain: project-level validation.
# Tests validation failure for negative possible error code.
autosar:
  version: "4.2"
  rootPackage: "DEMO"

inputs:
  baseTypes: "../platform/base_types.yaml"
  implementationDataTypes: "../types/implementation_types.yaml"
  applicationDataTypes: "../types/application_types.yaml"
  units:
    - "../units/*.yaml"
  compuMethods:
    - "../compu_methods/*.yaml"
  interfaces:
    - "../interfaces/If_VehicleSpeed.yaml"
    - "interfaces/If_Diagnostics_negative_error_code.yaml"
  swcs:
    - "../swcs/*.yaml"
  system: "../system.yaml"


