# ARForge: Invalid example fixture
# Domain: software component validation.
# Tests validation failure for bad runnable access.
swc:
  name: "SpeedConsumer"
  runnables:
    - name: "Runnable_UseSpeed"
      timingEventMs: 10
      reads:
        - port: "Rp_VehicleSpeed"
          dataElement: "VehicleSpeed_Invalid"
      writes:
        - port: "Rp_VehicleSpeed"
          dataElement: "VehicleSpeed"
      calls:
        - port: "Rp_Diag"
          operation: "ReadDTC"
  ports:
    - name: "Rp_VehicleSpeed"
      direction: "requires"
      interfaceRef: "If_VehicleSpeed"
    - name: "Rp_Diag"
      direction: "requires"
      interfaceRef: "If_Diagnostics"


