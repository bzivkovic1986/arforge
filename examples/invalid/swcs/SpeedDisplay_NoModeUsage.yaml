# ARForge: Invalid example fixture
# Domain: software component validation.
# Tests warning for declared mode-switch requires port with no runnable modeSwitchEvents.
swc:
  name: "SpeedDisplay"
  description: "Reads speed but does not react to mode changes."
  runnables:
    - name: "Runnable_ReadVehicleSpeed"
      timingEventMs: 10
      reads:
        - port: "Rp_VehicleSpeed"
          dataElement: "VehicleSpeed"
  ports:
    - name: "Rp_VehicleSpeed"
      direction: "requires"
      interfaceRef: "If_VehicleSpeed"
    - name: "Rp_PowerState"
      direction: "requires"
      interfaceRef: "If_PowerState"
