# ARForge: Invalid example fixture
# Domain: software component validation.
# Tests validation failure for diagrequiresonly.
swc:
  name: "DiagRequiresOnly"
  runnables:
    - name: "Runnable_Idle"
      timingEventMs: 10
  ports:
    - name: "Rp_Diag"
      direction: "requires"
      interfaceRef: "If_Diagnostics"


