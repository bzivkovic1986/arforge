# ARForge: Invalid example fixture
# Domain: software component validation.
# Tests validation failure for diagclientonly.
swc:
  name: "DiagClientOnly"
  runnables:
    - name: "Runnable_DiagClient"
      timingEventMs: 10
      calls:
        - port: "Rp_Diag"
          operation: "ReadDTC"
  ports:
    - name: "Rp_Diag"
      direction: "requires"
      interfaceRef: "If_Diagnostics"


