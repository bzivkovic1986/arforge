# ARForge: Invalid example fixture
# Domain: project-level validation.
# Tests validation failure for missing cs server binding.
autosar:
  version: "4.2"
  rootPackage: "DEMO"

inputs:
  baseTypes: "../platform/base_types.yaml"
  implementationDataTypes: "../types/implementation_types.yaml"
  applicationDataTypes: "../types/application_types.yaml"
  interfaces:
    - "../interfaces/*.yaml"
  swcs:
    - "swcs/SpeedSensor_missing_cs_binding.yaml"
    - "../swcs/SpeedConsumer.yaml"
  system: "../system.yaml"


