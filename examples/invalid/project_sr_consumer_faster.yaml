# ARForge: Invalid example fixture
# Domain: project-level validation.
# Exercises SR timing mismatch where the consumer runs faster than the producer.
autosar:
  version: "4.2"
  rootPackage: "DEMO"

inputs:
  baseTypes: "support/base_types.yaml"
  implementationDataTypes: "support/implementation_types.yaml"
  applicationDataTypes: "support/application_types.yaml"
  units:
    - "support/units.yaml"
  compuMethods:
    - "support/compu_methods.yaml"
  interfaces:
    - "../interfaces/*.yaml"
  swcs:
    - "swcs/SpeedSensor_timing_20ms.yaml"
    - "../swcs/SpeedConsumer.yaml"
  system: "support/system_consumer_pair.yaml"
