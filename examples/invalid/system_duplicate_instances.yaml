# ARForge: Invalid example fixture
# Domain: project-level validation.
# Tests validation failure for duplicate instances.
system:
  name: "BadSystem_DuplicateInstances"
  composition:
    name: "Composition_BadSystem_DuplicateInstances"
    components:
      - name: "SpeedSensor_1"
        typeRef: "SpeedSensor"
      - name: "SpeedSensor_1"
        typeRef: "SpeedConsumer"
    connectors:
      - from: "SpeedSensor_1.Pp_VehicleSpeed"
        to: "SpeedSensor_1.Rp_VehicleSpeed"


