# ARForge: Invalid example fixture
# Domain: project-level validation.
# Tests validation failure for constraint uint16 negative min.
autosar:
  version: "4.2"
  rootPackage: "DEMO"

inputs:
  baseTypes: "platform/base_types_split.yaml"
  implementationDataTypes: "types/implementation_types_split.yaml"
  applicationDataTypes: "types/application_types_constraint_uint16_negative_min.yaml"
  interfaces:
    - "../interfaces/*.yaml"
  swcs:
    - "../swcs/*.yaml"
  system: "../system.yaml"


