autosar:
  version: "4.2"
  rootPackage: "DEMO"

inputs:
  datatypes: "../datatypes.yaml"
  interfaces:
    - "../interfaces/*.yaml"
  swcs:
    - "../swcs/*.yaml"
  system: "system_sr_unknown_dataelement.yaml"
