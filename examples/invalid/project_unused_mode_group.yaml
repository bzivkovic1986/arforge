# ARForge: Invalid example fixture
# Domain: mode declaration groups.
# Tests warning for a declared ModeDeclarationGroup that is not referenced by any ModeSwitchInterface.
autosar:
  version: "4.2"
  rootPackage: "DEMO"

inputs:
  baseTypes: "../types/base_types.yaml"
  implementationDataTypes: "../types/implementation_types.yaml"
  applicationDataTypes: "../types/application_types.yaml"
  units:
    - "../units/*.yaml"
  compuMethods:
    - "../compu_methods/*.yaml"
  modeDeclarationGroups:
    - "../modes/power_state.yaml"
    - "modes/unused_mode_group.yaml"
  interfaces:
    - "../interfaces/If_VehicleSpeed.yaml"
    - "../interfaces/If_PowerState.yaml"
  swcs:
    - "../swcs/SpeedSensor.yaml"
    - "../swcs/SpeedDisplay.yaml"
  system: "../system.yaml"
